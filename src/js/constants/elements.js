const popupContainer = document.querySelector('.popup');
const menuContainer = document.querySelector('.header__menu');
const loginButtonClass = 'button_type_login';

export { popupContainer, menuContainer, loginButtonClass };
